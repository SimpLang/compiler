#ifndef COMPILER_H
#define COMPILER_H

// Include the necessary headers
#include "tokenizer.h"

// Function to compile source code
void compile(char* sourceCode);
void println();
#endif // COMPILER_H
