#ifndef FUNCTIONS_H
#define FUNCTIONS_H


#include "tokenizer.h"

// Function prototypes
void executePrintln(Token* tokens); // Added
int isFunction(const char* token);
void println(const char* args);

#endif // FUNCTIONS_H
